# SPA fallback — Cloudflare Pages serves static files first, so prerendered
# routes (due-diligence/index.html, dashboard/index.html, etc.) are served
# directly. This catch-all only handles routes with no static file.
/*  /index.html  200
