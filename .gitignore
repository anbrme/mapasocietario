node_modules
dist
.DS_Store
*.local
/.sync

# Private confirmation evidence log — contains personal data, never commit
confirmation-evidence.local.*

# Local env overrides (branch/dev backend URLs) — never commit
.env.local
.env*.local
.superpowers/
.claude/worktrees/agent-ad36e71f8beb02e63/
