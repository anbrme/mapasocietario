node_modules
dist
.DS_Store
*.local
/.sync

# Android / Capacitor build artifacts (regenerated by `npx cap sync` + gradle)
android/app/build
android/build
android/.gradle
android/capacitor-cordova-android-plugins/build

# Private confirmation evidence log — contains personal data, never commit
confirmation-evidence.local.*

# Local env overrides (branch/dev backend URLs) — never commit
.env.local
.env*.local
.superpowers/
.claude/worktrees/agent-ad36e71f8beb02e63/
batch-promotion-out/
batch2-out/
resync-out/

# Local wrangler state (pages dev / d1 simulation)
.wrangler

# Deck/report generation scratch: pptx+pdf build artifacts, chart-data
# snapshots, and the one-off .mjs scripts that produced them. All regenerable,
# and a single run adds ~16MB across ~145 files. tmp/vc-deck was committed by
# an earlier round before this rule existed.
.chart-data-*/
output/presentations/
tmp/vc-deck*/

# Local Pages dev secrets (VERIFY_ADMIN_TOKEN etc). Never committed.
.dev.vars
.dev.vars.*
.wrangler/state/v3/cache/miniflare-CacheObject/metadata.sqlite-wal
.dev.vars
